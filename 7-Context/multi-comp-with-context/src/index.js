import React, {userStete} from 'react';
import ReactDOM from 'react-dom/client';

import App from './App';
//import {UserContext} from './context/context'



const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
